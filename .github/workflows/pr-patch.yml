name: Commit PR Patch to Default Branch

on:
  pull_request_target:
    types: [ready_for_review, labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  commit-patch:
    if: >
      github.event.action == 'ready_for_review' ||
      (github.event.action == 'labeled' && github.event.label.name == 'needs-review')
    runs-on: ubuntu-latest

    env:
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PATCH_FILE: pr-reviews/pr-${{ github.event.pull_request.number }}.patch
      PATCH_URL: https://patch-diff.githubusercontent.com/raw/${{ github.repository }}/pull/${{ github.event.pull_request.number }}.patch

    steps:
      - name: Checkout default branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure folder exists
        run: mkdir -p pr-reviews

      - name: Download patch
        run: |
          curl -fsSL "$PATCH_URL" -o "$PATCH_FILE"
          test -s "$PATCH_FILE" || { echo "Empty patch file"; exit 1; }

      - name: Commit patch to default branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$PATCH_FILE"
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "chore(pr-reviews): update patch for PR #$PR_NUMBER"
          git push